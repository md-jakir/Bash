#!/bin/bash
#read -p "Enter your option : " find
path=/jakir/script/test
cd $path
#shopt -s dotglob
#shopt -s nullglob
array=(*/)
MAX=2
for ((arr=0; arr<=MAX; arr++)); do 
#if [ "$find" = "dir" ]; then
#for dir in "${array[@]}"; do	
#if [ -d "${array}" ]; then
	#tar -czf $f.tgz $f
done
#echo ${array[0]}
#done
#fi
#ps3="which dir do you want? "
#echo "There are ${#array[@]} dirs in current path"; \
#	select dir in "${array[@]}"; do echo "you selected ${dir}"'!'; break; done


